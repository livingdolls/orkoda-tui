name: Apply executor failure restart fix

on:
  push:
    branches:
      - fix/executor-failure-restart
    paths:
      - scripts/fix_executor_failure_restart.py
      - .github/workflows/apply-executor-failure-fix.yml

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix/executor-failure-restart
          fetch-depth: 0
      - uses: actions/setup-go@v5
        with:
          go-version: 1.26.x
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.14
      - run: bun install
      - name: Apply fix
        run: python3 scripts/fix_executor_failure_restart.py
      - name: Format
        run: |
          gofmt -w cmd internal
          bunx biome check --write apps/tui/src
      - name: Validate
        run: |
          go test ./...
          bun run lint:ts
          bun run typecheck
          bun run test:ts
      - name: Commit fix
        run: |
          rm -f scripts/fix_executor_failure_restart.py
          rm -f .github/workflows/apply-executor-failure-fix.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "fix: make failed Executor workflows restartable" || exit 0
          git push origin HEAD:fix/executor-failure-restart
